{
  "name": "@0y0/scraper-plugin-http",
  "version": "1.0.0",
  "description": "A @0y0/scraper plugin to handle http input and output.",
  "main": "index.js",
  "repository": "https://github.com/o0y0o/web-scraper",
  "author": "Jason Chung",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "web-scraper",
    "scraper",
    "http"
  ],
  "scripts": {
    "format": "prettier --write *.js",
    "lint": "eslint *.js",
    "pkg": "[ $(yarn info $npm_package_name version) != $npm_package_version ] && yarn publish || echo Skip publishing due to v$npm_package_version exist"
  },
  "dependencies": {
    "@0y0/exec-http": "^1.0.0"
  }
}
