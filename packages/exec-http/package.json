{
  "name": "@0y0/exec-http",
  "version": "1.0.1",
  "description": "Run the http request and aggregate the results.",
  "main": "index.js",
  "repository": "https://github.com/o0y0o/web-scraper",
  "author": "Jason Chung",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "http",
    "exec",
    "aggregate",
    "vm"
  ],
  "scripts": {
    "format": "prettier --write {*,lib/*}.js",
    "lint": "eslint {*,lib/*}.js",
    "test": "jest",
    "pkg": "[ $(yarn info $npm_package_name version) != $npm_package_version ] && yarn publish || echo Skip publishing due to v$npm_package_version exist"
  },
  "dependencies": {
    "@0y0/exec": "^1.0.2",
    "form-data": "^4.0.0",
    "node-fetch": "^2.6.7"
  }
}
