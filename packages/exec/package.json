{
  "name": "@0y0/exec",
  "version": "1.0.0-rc.2",
  "description": "Execute code and get the return.",
  "main": "index.js",
  "repository": "https://github.com/o0y0o/web-scraper",
  "author": "Jason Chung",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "compile",
    "execute",
    "run",
    "javascript",
    "code",
    "vm"
  ],
  "scripts": {
    "format": "prettier --write *.js",
    "lint": "eslint *.js",
    "test": "jest",
    "pkg": "[ $(yarn info $npm_package_name version) != $npm_package_version ] && yarn publish || echo Skip publishing due to v$npm_package_version exist"
  },
  "babel": {
    "presets": [
      "@0y0/vanilla"
    ]
  }
}
